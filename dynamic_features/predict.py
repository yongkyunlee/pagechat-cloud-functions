import os
from sentence_transformers import SentenceTransformer
from model 



def initialize(model_path):
	model = TheModelClass(*args, **kwargs)
	model.load_state_dict(torch.load(PATH))
	model.eval()

def update(new_title, h, c, model):
	encode
